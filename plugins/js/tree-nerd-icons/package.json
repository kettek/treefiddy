{
  "name": "treefiddy-plugin-test",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "mappings": "bun run get_mappings.ts",
    "build": "bun run mappings && bun build --minify --outdir dist ./index.ts",
    "install": "bun run build && rm -rf $(bun run ../get_install_dir.ts) && cp -rfv dist $(bun run ../get_install_dir.ts)"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@folder/xdg": "^4.0.1",
    "@m234/nerd-fonts": "^0.4.0"
  }
}
